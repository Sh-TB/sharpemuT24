# EXP-101 — All 5 PLT Stubs SUCCEED — Callback IS Stored — Case B Confirmed

**Identity:** Yatzi / PPSA17697 / verified
**Configuration:** `SHARPEMU_SEMA_FAST_PATH=0`, metadata at `Media/Metadata/`, all prior tracers active
**Path:** B (real metadata path)

**Question:** Do all 5 PLT stubs inside the registration helper return 0 (success), or does one return failure and skip callback storage?

**Answer:** **Case B confirmed.** All 3 PLT stubs reached inside `0x804F889D0` return `eax=0` (SUCCESS). The callback IS stored via `xchg [r14], rax` at `0x804F88A76`. The 2 PLT stubs in `0x804FA8490` were not reached (the code took a different branch — `r15 == -1` at `0x804F88A12`, skipping `0x804FA8490`). The callback is stored correctly. The mystery is now purely invocation — the callback exists but is never called.

**Confidence:** PROVEN by runtime tracing (INT3 before/after each PLT call, return values captured).

---

## Hypothesis

One of the 5 PLT stubs inside the registration helper returns non-zero, causing the callback storage at `0x804F88A76: xchg [r14], rax` to be skipped.

## Test Method

Built `_Exp101PLTStubTracer.cs` — patches all 5 call sites with INT3, captures input registers and return values (eax).

## Runtime Evidence

### Sites 2, 3, 4 (in `0x804F889D0` — registration helper): ALL SUCCEED

```
[EXP101-CALL] #1 site[2] PLT=0x804FC33C0 — registration #1
  rdi=0x6FFFF01FB930 rsi=0x0 rdx=0x6FFFF01FB940 rcx=0x6FFFF01FB920
[EXP101-RET] site[2] eax=0x0  *** SUCCESS ***

[EXP101-CALL] #2 site[3] PLT=0x804FC33D0 — registration #2
[EXP101-RET] site[3] eax=0x0  *** SUCCESS ***

[EXP101-CALL] #3 site[4] PLT=0x804FC33E0 — registration #3
[EXP101-RET] site[4] eax=0x0  *** SUCCESS ***
```

### Sites 0, 1 (in `0x804FA8490` — callback storage): NOT REACHED

`0x804FA8490` was never called. The code at `0x804F88A12: cmp r15, -1; je 0x804F88A28` took the `je` branch (r15 == -1), skipping the call to `0x804FA8490`. This is a different code path — `0x804FA8490` is only called when `r15 != -1` (when a specific flag is set). In this run, the flag is -1, so `0x804FA8490` is correctly skipped.

### Code Path After All 3 PLT Stubs Succeed

```asm
0x804F88A6C: (return from PLT 0x804FC33E0 — eax=0)
0x804F88A6E: jne 0x804F88A79  ; NOT taken (eax=0)
0x804F88A70: mov rax, [rbp-0x30]  ; load the callback pointer
0x804F88A74: xor ebx, ebx        ; clear error flag
0x804F88A76: xchg [r14], rax     ; *** STORE CALLBACK *** ← this executes!
0x804F88A79: mov rax, [r12]      ; stack canary check
0x804F88A7D: cmp rax, [rbp-0x28]
0x804F88A81: jne error            ; canary check
0x804F88A83: mov eax, ebx         ; return 0 (success)
0x804F88A91: ret
```

The callback IS stored at `[r14]` via `xchg`. The registration function returns 0 (success).

## PLT Stub NID Decoding

The PRX's `.dynstr` does not contain readable NID strings (PS5 PRXs use a different NID encoding). The NIDs are resolved at runtime by SharpEmu's import resolver. The PLT stubs' GOT slots are:

| PLT Stub | GOT Slot | PLT Index | NID | Status |
|----------|----------|-----------|-----|--------|
| `0x804FC33C0` | `0x8089243D0` | 164 | (runtime-resolved) | **SUCCESS** |
| `0x804FC33D0` | `0x8089243D8` | 165 | (runtime-resolved) | **SUCCESS** |
| `0x804FC33E0` | `0x8089243E0` | 166 | (runtime-resolved) | **SUCCESS** |
| `0x804FC36F0` | `0x808924568` | 215 | (not reached) | N/A |
| `0x804FC3700` | `0x808924570` | 216 | (not reached) | N/A |

## Conclusion

**Hypothesis REJECTED.** All 3 PLT stubs in the registration helper succeed. The callback IS stored via `xchg [r14], rax` at `0x804F88A76`. The registration function returns 0 (success).

**This is Case B:** The callback is stored correctly. The mystery is now purely invocation — the callback exists but is never called by the ThreadPool work-submission path.

## What Was Rejected

1. "One PLT stub returns failure" — REJECTED (all 3 return 0)
2. "Callback storage is skipped" — REJECTED (xchg executes)
3. "0x804FA8490 is required for callback storage" — REJECTED (not reached, different code path)

## What SharpEmu Is Missing

The callback IS registered and stored. The issue is **downstream of registration**:
- The callback pointer is stored at `[r14]` (inside the IL2CPP context structure)
- But no code path ever reads `[r14]` and calls the callback
- The ThreadPool work-submission function (`0x804F6EC20`) is still never reached (EXP-096)
- The work-submission call chain is still dead code (EXP-096/097)

The callback registration succeeded, but the code that should INVOKE the callback (submit work to the ThreadPool) is still unreachable.

## Next Exact Step

**EXP-102:** The callback is stored at `[r14]` inside the registration function. Trace what `r14` points to and where the callback pointer ends up. Then find what code should read that pointer and call it — this is the missing invocation path.

Specifically:
1. What is `[r14]`? (The IL2CPP context structure field where the callback is stored)
2. After `xchg [r14], rax`, what value is in `r14`? (The address where the callback was stored)
3. Search for reads from that address — what code should load the callback and call it?
4. Is the callback pointer ever loaded by the ThreadPool dispatch function (`0x804F6E510`)?

One question: **Where is the callback pointer stored (what address), and what code should read it to invoke the callback?**

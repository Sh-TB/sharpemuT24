# EXP-074 — Check Rendering Progress After SignalSema Fix

## Date: 2026-07-31
## Status: COMPLETE — Game does NOT reach rendering; workers still spin on unsignaled semaphore

---

## BOOT IDENTITY REPORT

**Game:** Yatzi (PPSA17697)
**eboot SHA256:** d17fba4abc7858495c6f6e207b5c38961eec0c4639b04369f0c7b06866d80b6c
**Consistency:** PASS

---

## Question

**Did Yatzi reach rendering after the SignalSema fix (11-byte NOP)?**

## Answer

**NO.** The game does not reach VideoOut, GPU, or any rendering path.

---

## Evidence

### Rendering calls: ALL ZERO

| Function | Calls |
|----------|-------|
| sceVideoOutOpen | 0 |
| sceVideoOutSubmitFlip | 0 |
| sceVideoOutSubmitFrame | 0 |
| sceGnmSubmitCommandBuffer | 0 |
| sceAgcDriverSubmitDcb | 0 |
| sceAgcDraw / sceAgcDispatch | 0 |
| Present / Swapchain / Framebuffer | 0 |

### AGC/GPU activity: NONE

- 0 sceAgcCreateShader calls
- 0 sceAgcCreatePrimState calls
- 0 sceAgcDriverSubmitDcb calls
- 2 sceKernelAllocateDirectMemory calls (memory allocated but not for GPU)

### SignalSema handle diversity (User Feedback #2)

**SignalSema IS being called (13,141 times) but NOT on the handle workers wait on:**

| Operation | Handle | Count | Caller |
|-----------|--------|-------|--------|
| WaitSema (workers) | 0x5C | 42 | 0x800AA0207 (worker task loop) |
| WaitSema (main thread) | 0x5D, 0x5F, 0x61, 0x63, 0x65, 0x67 | 1 each | 0x800A9FC25 |
| SignalSema | ? | 13,141 | NOT on handle 0x5C |

**The 13,141 SignalSema calls are NOT signaling the workers' semaphore (0x5C).** Workers spin on 0x5C; SignalSema signals other handles.

### Is this a spin pattern? (User Feedback #2)

**YES** — SignalSema is being called 13K times but on wrong handles. Workers still spin on 0x5C. This is a **new spin pattern** — the game appears active (13K SignalSema calls, 2M+ imports) but workers are still not receiving tasks.

---

## Task 3: Compare with Previous Blocker

### Is this the same synchronization issue?

**YES, same class.** Workers wait on a semaphore (0x5C) that nobody signals. The 11-byte NOP fixed the IMMEDIATE gate (dependency check at `[rbx+0x108]`) but the underlying dependency resolution mechanism is still missing. The game progresses further (32M imports vs 100K) but still can't dispatch tasks to workers because the dependency at `[rbx+0x108]` is bypassed, not resolved.

### What changed vs before?

| Metric | Before NOP | After NOP |
|--------|-----------|----------|
| NULL executes | 1,005 | 0 |
| SIGABRT | 1 | 0 |
| SignalSema calls | 0 | 13,141 (wrong handles) |
| Workers spin on | 0x5C (NULL call) | 0x5C (WaitSema spin) |
| Rendering reached | No | No |

The NOP eliminated the NULL execute cascade (good) but didn't fix the root cause: workers' semaphore (0x5C) is never signaled.

---

## Layer Status

| Layer | Status |
|-------|--------|
| Layer 1 — Dump | ✅ SOLVED |
| Layer 2 — Loader | ✅ SOLVED |
| Layer 3 — Runtime | ⚠️ PARTIAL (SignalSema fires but on wrong handles; workers still spin) |
| Layer 4 — Unity | ❌ NOT REACHED (no VideoOut, no GPU) |
| Layer 5 — Game Logic | ❌ NOT REACHED |

---

## Remaining Blocker

**Workers wait on semaphore handle 0x5C but nobody signals it.** The 11-byte NOP bypasses the dependency gate but doesn't resolve the dependency — SignalSema fires on other handles (main thread's handles) but never on 0x5C (workers' handle). The dependency resolution mechanism that should clear `[rbx+0x108]` and trigger the CLEAR function (`0x800A9F750`) to signal handle 0x5C is still missing.

---

## Request Summary

- **Total requested:** 5
- **Completed:** 5 (rendering search, AGC check, comparison, layer update, blocker identification)
- **Failed:** 0
- **Remaining:** 0
- **Current blocker:** Workers spin on semaphore 0x5C — never signaled
- **Next EXP:** EXP-075 — Find what should signal semaphore handle 0x5C; the dependency at `[rbx+0x108]` must be properly resolved (not bypassed) for the correct SignalSema handle to be used

---

## Documentation

- **Commit hash:** (to be filled)
- **GitHub link:** https://github.com/Sh-TB/sharpemuT24
- **Diagnostic file:** `docs/diagnostics/EXP-074.md`
- **Runtime log:** `/tmp/exp073_logs/run1_11nop.log` (39,749 lines)

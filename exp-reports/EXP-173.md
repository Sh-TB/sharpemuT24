# EXP-173 RESULT

## 1. Confirmed Execution Sequence (with argc=2)

```
Loader → DT_INIT → 0x8013FCE40 (argc=2, jl NOT taken)
  → 0x8013EB6B0 (consumer entry, [0x801E518C8]=0x20000259C0)
  → Consumer passes branches: +0x72 through +0x191F (all NOT taken)
  → +0x19A7: indirect call [0x801ED6320] (GOT-resolved function)
  → +0x19B1: je (checks return value) — NOT taken when INT3 present
  → Consumer exits between +0x191F and +0x19B3 WITHOUT reaching +0x3969
  → Dispatch loop → WaitSema(0x81) → DEADLOCK
```

## 2. Exact First Failure Point

**The indirect call at +0x19A7 (0x8013ED057): `call [rip+0xAE92C3]` (GOT address 0x801ED6320)**

This is the last instruction reached before the consumer exits. The call goes through a GOT slot that is resolved at runtime by relocation. The function called through this GOT slot either:
- Does not return (enters the dispatch loop directly)
- Returns a value that causes a subsequent branch to skip the init write

## 3. Instruction/Address Causing Failure

| Address | Instruction | Description |
|---------|-------------|-------------|
| 0x8013ED057 (+0x19A7) | `call [rip+0xAE92C3]` | Indirect call through GOT at 0x801ED6320 |
| 0x8013ED061 (+0x19B1) | `je +0x19CC` | Checks return value of indirect call |

## 4. Register Values

**At indirect call (+0x19A7), with INT3:**
```
rax=0x000000060000007F
rbx=0x00000006006C49F0
r14=0x0000000804CD5230
[0x801E518C8]=0x00000020000259C0
[0x801E51240]=0x0000000000000000
```

**After indirect call, at je (+0x19B1), with INT3:**
```
rax=0x0000000000AE9342 (return value — non-zero)
rbx=0x0000000000AE9342 (mov ebx, eax)
[0x801E51240]=0x0000000000000000 (STILL NULL)
```

**Without INT3 at +0x19A7:** je at +0x19B1 is NOT reached — function exits before it.

## 5. Memory Values

| Address | Before indirect call | After indirect call |
|---------|---------------------|-------------------|
| [0x801E518C8] | 0x00000020000259C0 | 0x00000020000259C0 (unchanged) |
| [0x801E51240] | 0x0 (NULL) | 0x0 (STILL NULL) |
| [0x801E50DF0] | 0x801BB4B77 | 0x801BB4B77 (unchanged) |
| [0x801E50DF8] | 0x801E518C8 | 0x801E518C8 (unchanged) |

## 6. Closed Hypotheses

| Hypothesis | Status | Evidence |
|-----------|--------|----------|
| TLS initialization | CLOSED | REJECTED (EXP-172) |
| Initializer ordering | CLOSED | REJECTED (EXP-167) |
| argc=1 | CLOSED | CONFIRMED as one blocker (EXP-169) |
| Consumer early exit at +0x72 | CLOSED | Fixed with argc=2 |
| Skip branches +0x277 to +0x191F | CLOSED | All NOT taken at runtime |
| [0x801E51240] has no writer | CLOSED | Writer exists at +0x3969 but never reached |

## 7. Next Evidence Target

**The indirect call at +0x19A7 (`call [0x801ED6320]`) is the critical exit point.**

Need to determine:
1. What function is stored in GOT slot 0x801ED6320 at runtime?
2. Does this function return, or does it enter the dispatch loop?
3. If it returns, what does the return value control?
4. Is this an HLE-resolved import or a native function?

The GOT slot at 0x801ED6320 is in BSS (filled by relocation at runtime). Need to check the runtime log for which import/relocation fills this slot, and what function it points to.

## Lifecycle Map

```
Loader                    ✅ PASS
DT_INIT                   ✅ PASS
0x8013FCE40 (argc=2)      ✅ PASS (jl not taken)
0x801E518C8 init          ✅ PASS (non-NULL with argc=2)
Consumer 0x8013EB6B0      ✅ PASS (entered, passes +0x72)
Consumer +0x191F           ✅ PASS (jne not taken)
Consumer +0x19A7           ❓ CRITICAL — indirect call through GOT
0x801E51240 init           ❌ FAIL (never reached, +0x3969 skipped)
PlayerLoop                 ❌ FAIL
VideoOut                   ❌ FAIL
WaitSema(0x81)             ❌ DEADLOCK
```

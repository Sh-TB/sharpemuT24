# EXP-142 — Resolver Full Regression Audit

**Date:** 2026-08-04
**Task ID:** 142
**Predecessor:** EXP-026 (original "232 NULL returns"), EXP-137 Phase 5 (root cause identified)
**Priority:** 🟡 P2 — closing the loop on EXP-026..EXP-137
**Status:** STATIC ANALYSIS COMPLETE — runtime tests PENDING (require EXP-138 fix)

## Goal

Per user:
> "بستن کامل EXP-026 تا EXP-137"
> (Fully close EXP-026 through EXP-137)

This EXP verifies the resolver works correctly after EXP-138 fix, ensuring no hidden RAX loss anywhere in the resolver → guest callback chain.

---

## Test Plan

### 142.1 Resolver call count — ⏸ PENDING (requires EXP-138 fix)
**Before EXP-138 fix:** 232 resolver calls, 232 return RAX=0 (per EXP-026)
**After EXP-138 fix (expected):** 232 resolver calls, ~232 return non-zero function pointers (some may legitimately return 0 for symbols Yatzi doesn't have)

**Counter to verify:** `_resolverReturnZero` and `_resolverReturnNonZero` at `DirectExecutionBackend.Imports.cs:2399, 2405`
- Before: `_resolverReturnZero=232`, `_resolverReturnNonZero=0`
- After (expected): `_resolverReturnZero≈0`, `_resolverReturnNonZero≈232`

### 142.2 Symbol list — ✅ STATIC-COMPLETE (from EXP-118 log)
The 232 symbols are all `il2cpp_*` API names. Key ones to verify post-fix:

| Symbol | Expected return | Notes |
|--------|-----------------|-------|
| `il2cpp_init` | 0x804ED85D0 | Real init function |
| `il2cpp_init_utf16` | 0x804ED??? | UTF16 variant |
| `il2cpp_shutdown` | 0x804ED???? | Shutdown |
| `il2cpp_get_corlib` | 0x804ED???? | Get corlib image |
| `il2cpp_add_internal_call` | 0x804ED8770 | icall registration |
| `il2cpp_resolve_icall` | 0x804ED8780 | icall resolution |
| `il2cpp_alloc` | 0x804ED???? | Allocator |
| `il2cpp_free` | 0x804ED???? | Deallocator |
| `il2cpp_class_get_fields` | 0x804ED???? | Class introspection |
| `il2cpp_class_num_fields` | 0x804ED???? | Class introspection |
| `il2cpp_array_length` | 0x804ED???? | Array |
| `il2cpp_gc_collect` | 0x804ED???? | GC |
| `il2cpp_gc_collect_a_little` | 0x804ED???? | GC incremental |
| `il2cpp_gc_disable` | 0x804ED???? | GC |
| `il2cpp_gc_enable` | 0x804ED???? | GC |
| `il2cpp_gc_get_heap_size` | 0x804ED???? | GC |
| `il2cpp_gc_get_used_size` | 0x804ED???? | GC |
| `il2cpp_gchandle_free` | 0x804ED???? | GC handle |
| `il2cpp_gchandle_get_target` | 0x804ED???? | GC handle |
| `il2cpp_gchandle_new` | 0x804ED???? | GC handle |
| `il2cpp_gchandle_new_weakref` | 0x804ED???? | GC handle |

### 142.3 Return propagation — ⏸ PENDING (requires EXP-138 fix)
For every nested guest callback (not just resolver):
- Guest return (host RAX after thunk)
- Host return (what `TryCallGuestFunction` returns)
- Caller result (what the outer guest sees in its RAX)

**Required instrumentation:**
- Log host RAX immediately after `CallNativeEntry` returns
- Log `context.Rax` value at `TryCallGuestFunction:3500`
- Log `returnValue` at `DispatchIl2CppApiLookupSymbol:2410`

**Success criterion:** All three values match (after EXP-138 fix).

---

## My Additional Considerations

### 142.4 Audit ALL `TryCallGuestFunction` callers
Per EXP-138, the bug affects every nested guest callback. Audit all callers:

```bash
grep -rn "TryCallGuestFunction" src/SharpEmu.Core/
```

Each caller should be verified to correctly receive and use the return value.

### 142.5 Audit `ExecuteGuestContinuationEntry` return path
Per EXP-138 static analysis, the continuation path has a manual `EmitMovR64Imm(0x48, 0xB8, ...)` at line 5176 to set Rax going IN. Need to verify it also captures Rax going OUT (post-thunk).

### 142.6 Test resolver with a synthetic BST
Per closed EXP-026/027/028 work: synthetic resolver test with 239 symbols, 239/239 resolved. This was done with Unicorn. After EXP-138 fix, re-run the same test with SharpEmu's actual resolver and verify 239/239 still pass.

### 142.7 Verify BST is correct (per closed EXP-026)
Per closed findings:
- 239 nodes
- 0 violation
- RB tree valid
- Inverted tree correct

These are CLOSED. No need to re-test.

### 142.8 Verify CPU instruction correctness (per closed EXP-027)
Per closed findings:
- cmovns, test, lea, branch logic
- 768/768 fuzz PASS

These are CLOSED. No need to re-test.

### 142.9 Verify Synthetic Resolver (per closed EXP-028)
Per closed findings:
- 239/239 symbol resolve

CLOSED. No need to re-test.

### 142.10 FAST_PATH hypothesis (per closed EXP-128/134)
Per closed findings:
- REJECTED

CLOSED. No need to re-test.

### 142.11 init_array / RELA (per closed EXP-132)
Per closed findings:
- 50,450 relocations applied
- RELA loading correct

CLOSED. No need to re-test (EXP-137 Phase 6-A re-confirmed).

---

## Status Summary

| Subtest | Status | Notes |
|---------|--------|-------|
| 142.1 Resolver call count | ⏸ PENDING | Needs EXP-138 fix |
| 142.2 Symbol list | ✅ STATIC-COMPLETE | 232 symbols, key ones listed |
| 142.3 Return propagation | ⏸ PENDING | Needs EXP-138 fix + instrumentation |
| 142.4 All TryCallGuestFunction callers | ⏸ PENDING | Needs EXP-138 fix |
| 142.5 Continuation return path | ⏸ PENDING | Needs EXP-138 fix |
| 142.6 Synthetic BST test | ⏸ PENDING | Needs EXP-138 fix |
| 142.7 BST correctness | ✅ CLOSED | Per EXP-026 |
| 142.8 CPU instruction correctness | ✅ CLOSED | Per EXP-027 (768/768 fuzz PASS) |
| 142.9 Synthetic Resolver | ✅ CLOSED | Per EXP-028 (239/239 resolve) |
| 142.10 FAST_PATH hypothesis | ✅ CLOSED | Per EXP-128/134 (REJECTED) |
| 142.11 init_array / RELA | ✅ CLOSED | Per EXP-132 (50,450 relocations applied) |

## Closure Statement

EXP-026 through EXP-137 investigated the "232 NULL returns" symptom from various angles:
- EXP-026: BST algorithm — CLOSED (algorithm correct)
- EXP-027: CPU instruction correctness — CLOSED (768/768 fuzz PASS)
- EXP-028: Synthetic resolver — CLOSED (239/239 resolve)
- EXP-035..EXP-078: IL2CPP metadata, threading, semaphores — CLOSED (downstream of root cause)
- EXP-079..EXP-110: Callback dispatch, trigger hunt — CLOSED (downstream of root cause)
- EXP-111..EXP-117: Asset verification, claim validation — CLOSED (real Unity resources needed)
- EXP-118..EXP-135: WaitSema(0x81) deadlock analysis — CLOSED (root cause found in EXP-137)
- EXP-136: arch_init_gc identified — OPEN (gated on EXP-138)
- EXP-137: TryCallGuestFunction RAX bug — ROOT CAUSE FOUND
- EXP-138: Fix design — PATCH DESIGNED, awaiting rebuild
- EXP-142 (this EXP): Closure verification — PENDING EXP-138 fix

**Root cause chain (final):**
```
TryCallGuestFunction RAX propagation bug (EXP-137 Phase 5)
   ↓
il2cpp_* API lookups return 0 (EXP-026 symptom)
   ↓
il2cpp_resolve_icall returns 0 for Unity Job System icalls
   ↓
JobHandle.Schedule() silently no-ops
   ↓
Bootstrap job never submitted
   ↓
Semaphore 0x81 never signaled
   ↓
Main thread blocks on WaitSema(0x81)
   ↓
System-wide deadlock
```

Once EXP-138 fix is applied and verified, this entire chain should collapse (assuming no other independent blockers like `arch_init_gc`).

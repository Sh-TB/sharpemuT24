# EXP-143 — IL2CPP Runtime Initialization Audit

**Date:** 2026-08-04
**Task ID:** 143
**Predecessor:** EXP-137 Phase 6-D (PRX module_start confirmed working)
**Priority:** 🟢 P3 — after EXP-138, EXP-139
**Status:** STATIC ANALYSIS COMPLETE — runtime tests PENDING

## Goal

Verify the complete IL2CPP runtime initialization sequence works correctly after EXP-138 fix.

---

## Test Plan

### 143.1 global-metadata.dat — ✅ COMPLETE (from EXP-060)
Per EXP-060 (CONFIRMED):
- File present: `global-metadata.dat` (10.7 MB)
- Magic verified: 0xFAB11BAF (IL2CPP metadata magic)
- Location: `/tmp/exp125_games/yatzi/global-metadata.dat`

### 143.2 CodeRegistration — ✅ STATIC-COMPLETE (from EXP-054, EXP-059)
Per EXP-054 (later re-identified by EXP-059):
- CodeRegistration struct found at `0x8086E9000` in guest memory
- Re-identified by EXP-059 as `Il2CppCodeGenModule` (not `Il2CppCodeRegistration` proper)
- `types[]` array at `0x80893E950`
- Call #7 = `methodPointers[0]`

**Open question after EXP-138:** Does the CodeRegistration get correctly populated after the resolver fix?

### 143.3 MetadataRegistration — ✅ STATIC-COMPLETE (from EXP-055)
Per EXP-055:
- MetadataRegistration found at `0x80885C580`
- Contains 3 extra PS5 code pointers (vs upstream Unity IL2CPP)
- Populated via relocations (per EXP-056)

**Open question after EXP-138:** Does the MetadataRegistration get correctly accessed after the resolver fix?

### 143.4 il2cpp_codegen_register — ✅ STATIC-COMPLETE (from EXP-052, EXP-053)
Per EXP-052/EXP-053:
- Wrapper at `0x800805AE0` = `il2cpp_codegen_register`
- Originally claimed static table at `0x1CC0080` was `Il2CppMetadataRegistration` — REFUTED by EXP-053 (it's a string fragment pool)
- Wrapper & insert function NEVER called (0 hits in runtime trace)

**Open question after EXP-138:** With the resolver fix, does the wrapper get called?

### 143.5 Static constructors — ⏸ PENDING (requires EXP-138 fix)
Per Unity IL2CPP source patterns, static constructors (`__cxa_*` / `__static_initialization_and_destruction_*`) are invoked via `il2cpp_runtime_class_init`. This icall is resolved via `il2cpp_resolve_icall` — currently returns 0 (broken).

After EXP-138 fix, verify static constructors actually execute.

### 143.6 init_array — ✅ COMPLETE (from EXP-137 Phase 6-C, 6-D)
Per EXP-137:
- PRXs use DT_INIT (module_start), NOT DT_INIT_ARRAY
- All 3 PRXs (libc.prx, libSceNpCppWebApi.prx, Il2cppUserAssemblies.prx) had module_start successfully dispatched
- All returned 0
- Real IL2CPP BST initialization code executed (FLAG-WATCH trace at exp118_run.log:932)

**This was previously thought broken (EXP-055) but EXP-137 overturned that conclusion.**

### 143.7 IL2CPP runtime ready — ⏸ PENDING (requires EXP-138 fix)
After all the above, the IL2CPP runtime should be in a "ready" state:
- BST populated with all `il2cpp_*` API symbols
- icall table populated with all `Unity.*::*` icalls
- GC initialized (or stubbed via EXP-139)
- CodeRegistration/MetadataRegistration accessible

**Success criterion:** Yatzi's C# bootstrap code can call `il2cpp_resolve_icall("Unity.Jobs.LowLevel.Unsafe.JobsUtility::Schedule_Injected")` and get a real function pointer back.

---

## My Additional Considerations

### 143.8 IL2CPP API GOT population
Per EXP-118 log, the IL2CPP runtime makes ~200 `il2cpp_api_lookup_symbol` calls to populate the IL2CPP API GOT. After EXP-138 fix, each call should return a real function pointer (currently all return 0).

**Test:** After fix, dump the GOT and verify it contains non-zero function pointers for all 232 il2cpp_* API entries.

### 143.9 IL2CPP icall table population
After the GOT is populated, Yatzi's init code should call `il2cpp_add_internal_call(name, fnptr)` for each Unity C# icall. This populates Yatzi's internal icall table.

**Test:** After fix, hook `il2cpp_add_internal_call` (or Yatzi's implementation at `0x804ED8770`) to log every (name, fnptr) pair registered. Verify that Job System icalls (`Schedule_Injected`, `ScheduleBatchedJobs`, `ResetJobWorkerCount`) are registered.

### 143.10 IL2CPP runtime invoke
Per user's EXP-139.7: `il2cpp_runtime_invoke` should work after fix. Test:
- method pointer
- object
- arguments
- exception pointer
- return value

### 143.11 Memory layout verification
Verify the IL2CPP memory layout is correct:
- Managed heap allocated via `sceKernelAllocateDirectMemory` (line 8313)
- TLS slots allocated
- Stack pools allocated
- GC roots registered

---

## Status Summary

| Subtest | Status | Notes |
|---------|--------|-------|
| 143.1 global-metadata.dat | ✅ COMPLETE | 10.7MB, magic 0xFAB11BAF |
| 143.2 CodeRegistration | ✅ STATIC, runtime PENDING | At 0x8086E9000 (re-identified as Il2CppCodeGenModule) |
| 143.3 MetadataRegistration | ✅ STATIC, runtime PENDING | At 0x80885C580 |
| 143.4 il2cpp_codegen_register | ✅ STATIC, runtime PENDING | Wrapper at 0x800805AE0; was never called pre-fix |
| 143.5 Static constructors | ⏸ PENDING | Needs EXP-138 fix |
| 143.6 init_array | ✅ COMPLETE | PRXs use DT_INIT, all return 0 |
| 143.7 IL2CPP runtime ready | ⏸ PENDING | Needs EXP-138 fix |
| 143.8 IL2CPP API GOT population | ⏸ PENDING | Needs EXP-138 fix |
| 143.9 IL2CPP icall table population | ⏸ PENDING | Needs EXP-138 fix |
| 143.10 il2cpp_runtime_invoke | ⏸ PENDING | Needs EXP-138 fix |
| 143.11 Memory layout verification | ⏸ PENDING | Needs EXP-138 fix |

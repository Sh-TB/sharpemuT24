با توجه به کل مسیر SharpEmuT24 که ساختید، من فکر می‌کنم مشکل Harvest Days / Seeker / Yatzi دیگر **مرحله بوت اولیه نیست**. طبق لاگ‌هایی که گفتید الگو این است:

```
ELF Load ✅
Imports Resolve ✅
CRT/C++ ABI ✅
IL2CPP Load ✅
Unity Threads ✅
AssetGarbageCollectorHelper ✅
Static Init ⚠️
Semaphore / Thread Sync ❌
VideoOut ❌
First Frame ❌
```

یعنی احتمال زیاد مشکل در **Runtime Synchronization + Guest Thread Execution + Unity initialization barrier** است.

به تستر اصلی این متن را بده. (یک فایل دستورالعمل واحد باشد، دیتابیس لازم نیست)

---

# SHARPEMU BRINGUP LOOP ENGINE v1.0

## هدف: حل خودکار مشکل بازی تا First Frame

قوانین:

## قانون 1 — هر تغییر قبل از تست باید checkpoint شود

قبل از هر آزمایش:

```
git tag backup/before-exp-XXX
```

هیچ تستی بدون نقطه برگشت انجام نشود.

---

# قانون 2 — هیچ خطایی دور ریخته نشود

هر اجرا باید ذخیره شود:

```
KnowledgeTransfer/
 ├── GameName/
 │    ├── RUN_HISTORY.md
 │    ├── CRASH_REPORT.md
 │    ├── FIX_APPLIED.md
 │    ├── CURRENT_STATE.md
 │    └── latest.log
```

---

# قانون 3 — هر Crash باید تبدیل به Task شود

فرمت:

```
Problem:
Function:
NID:
Register:
Memory:
Phase:
Possible Cause:
Applied Fix:
Result:
```

---

# قانون 4 — فقط یک مشکل در هر آزمایش

اشتباه:

```
10 تغییر
build
test
```

درست:

```
یک تغییر
build
test
compare
```

---

# قانون 5 — قبل از Fix کردن Crash، ریشه پیدا شود

هر SIGSEGV:

بررسی:

```
RIP
RSP
RDI
RSI
RDX
Memory region
Guest/Host pointer
```

---

# قانون 6 — تست Host Pointer Leak

برای تمام توابع pointer-return:

لاگ اضافه شود:

```
RETURN POINTER CHECK

address:
type:
guest:
host:
mapped:
```

اگر:

```
0x55xxxxxxxx
```

بود:

مشکوک HOST POINTER

اگر:

```
0x8xxxxxxxx
```

بود:

Guest Memory

---

# قانون 7 — IL2CPP Initialization Tracking

ثبت شود:

```
IL2CPP START

Loading metadata
Creating threads
Static constructors
ExecuteOnce
TLS
Exception runtime
```

---

# قانون 8 — _Execute_once واقعی بررسی شود

مشکل احتمالی:

الان:

```
stub return success
```

ولی Unity انتظار دارد:

```
run callback once
block others
release waiting threads
```

تست:

پیاده سازی واقعی:

```
GuestThreadExecution.Scheduler.TryCallGuestFunction()
```

---

# قانون 9 — sceKernelWaitSema بررسی شود

چون چند بازی همینجا گیر می‌کنند.

بررسی:

```
sceKernelCreateSema
sceKernelWaitSema
sceKernelSignalSema
sceKernelPollSema
```

لاگ:

```
semaphore id
current count
requested count
thread id
owner
timeout
```

---

# قانون 10 — Deadlock Detector اضافه شود

اگر:

یک thread بیشتر از:

```
30 seconds
```

منتظر بود:

گزارش:

```
POSSIBLE DEADLOCK

Thread:
Waiting:
Semaphore:
Created by:
Last signal:
```

---

# قانون 11 — Thread Scheduler Trace

ثبت:

```
Thread Create
Thread Start
Thread Exit
Thread Wait
Thread Resume
```

---

# قانون 12 — TLS کامل بررسی شود

Unity شدیداً TLS استفاده می‌کند.

تست:

```
pthread_getspecific
pthread_setspecific
__tls_get_addr
```

ثبت:

```
TLS key
Thread
Address
Value
```

---

# قانون 13 — C++ Exception Runtime

بررسی:

```
__cxa_allocate_exception
__cxa_throw
__cxa_begin_catch
__cxa_end_catch
```

---

# قانون 14 — Memory Guard

قبل از memcpy/memset:

بررسی:

```
source mapped?
destination mapped?
size valid?
permission?
```

---

# قانون 15 — Heap Tracking

هر malloc:

ثبت:

```
address
size
caller
thread
```

هر free:

ثبت:

```
free address
allocated from
```

---

# قانون 16 — Missing NID Intelligence

هر NID:

ذخیره:

```
NID
name
calls count
first call
last call
return value
```

---

# قانون 17 — Return Value Validation

برای HLE:

بررسی:

```
NULL
negative
invalid pointer
wrong handle
```

---

# قانون 18 — AGC Pipeline

برای بازی‌های جدید:

بررسی:

```
sceAgcCreateShader
sceAgcCreatePipeline
sceAgcSubmitDcb
Dcb parser
Register state
Framebuffer write
```

---

# قانون 19 — VideoOut فقط بعد از GPU آماده شود

ثبت:

```
Swapchain created
Image acquired
Command submitted
Present called
```

---

# قانون 20 — First Frame Detector

هر اجرا:

جستجو:

```
non black pixels
framebuffer change
present event
```

---

# قانون 21 — Regression Test

بعد از هر Fix:

اجرا:

```
Dreaming Sarah
Arise
Harvest Days
Seeker
Yatzi
```

نباید بازی قبلی خراب شود.

---

# قانون 22 — AI Summary خروجی بدهد

آخر هر Loop:

فایل:

```
AI_CONTEXT_NEXT_RUN.md
```

بسازد.

شامل:

```
Current state
Solved problems
Remaining blockers
Next experiments
```

---

# قانون 23 — حلقه خودکار

فرآیند:

```
RUN GAME

↓

COLLECT LOG

↓

ANALYZE

↓

FIND ROOT CAUSE

↓

CREATE FIX PLAN

↓

APPLY ONE FIX

↓

BUILD

↓

TEST AGAIN

↓

UPDATE KNOWLEDGE
```

تا رسیدن به:

```
FIRST FRAME PRESENTED
```

---

# قانون 24 — هر موفقیت تبدیل به Knowledge شود

مثلا:

```
HarvestDays.md

Problem:
sceKernelWaitSema

Fix:
Real semaphore scheduler

Result:
VideoOut reached
```

---

# قانون 25 — هیچ وقت از صفر شروع نشود

همیشه:

```
LAST_KNOWN_GOOD_STATE
```

مبنا باشد.

---

# تست‌های اولویت‌دار من برای حل مشکل فعلی:

ترتیب پیشنهادی:

## EXP-009

### Semaphore Investigation

احتمال: ⭐⭐⭐⭐⭐

---

## EXP-010

### Real _Execute_once callback

احتمال: ⭐⭐⭐⭐⭐

---

## EXP-011

### Guest Thread Scheduler

احتمال: ⭐⭐⭐⭐

---

## EXP-012

### TLS + pthread کامل

احتمال: ⭐⭐⭐⭐

---

## EXP-013

### IL2CPP Static Constructor Trace

احتمال: ⭐⭐⭐⭐

---

## EXP-014

### Heap/Pointer Leak Detector

احتمال: ⭐⭐⭐

---

## EXP-015

### AGC Framebuffer Validation

احتمال: ⭐⭐⭐

---

خروجی نهایی هر تست فقط یک فایل بده:

```
KnowledgeTransfer/GameName/latest-transfer.md
```

که چت اصلی بتواند مستقیم بخواند و روی فورک اعمال کند.

---

به نظر من الان بزرگ‌ترین شانس حل سه بازی:

1. `sceKernelWaitSema`
2. `_Execute_once`
3. `GuestThreadExecution.Scheduler`

هستند، چون سه بازی مستقل همان نقطه گیر را نشان داده‌اند.
با توجه به کاری که تا الان کردید، مشکل اصلی این است که **تسترها فقط اجرا می‌کنند ولی یک چرخه علمی تشخیص → ثبت → تغییر → تست مجدد → انتقال دانش ندارند**. باید یک سیستم حلقه‌ای بسازید که بدون دیتابیس هم کار کند و همه چیز را داخل فایل نگه دارد.

برای نسخه اصلی (نه تستر) این قابلیت‌ها را اضافه کنید:

---

# دستور ساخت سیستم Bringup Loop + Knowledge Export

این متن را مستقیم به برنامه‌نویس اصلی بده:

```
هدف:
ساخت یک سیستم استاندارد Debug Bringup Loop برای SharpEmu که بدون دیتابیس کار کند و تمام دانش حل مشکل را داخل فایل ذخیره کند.

قوانین:

1. هر اجرای بازی باید یک Experiment ID داشته باشد.

مثال:
EXP-001
EXP-002
EXP-003


ساختار:

SharpEmu.Diagnostics/
├── Bringup/
│   ├── Experiments/
│   │   ├── EXP-001/
│   │   │   ├── input.json
│   │   │   ├── run.log
│   │   │   ├── crash-analysis.md
│   │   │   ├── fixes.md
│   │   │   └── result.md
│   │
│   └── CURRENT_STATE.md
│
├── KnowledgeTransfer/
│   ├── Games/
│   ├── FixHistory.md
│   ├── KnownIssues.md
│   └── Export.md
```

---

# قانون اصلی حلقه تست

```
RUN
 ↓
COLLECT LOG
 ↓
ANALYZE
 ↓
IDENTIFY ROOT CAUSE
 ↓
WRITE SOLUTION
 ↓
CREATE PATCH PLAN
 ↓
APPLY FIX
 ↓
BUILD
 ↓
RUN AGAIN
 ↓
COMPARE RESULT
```

تا وقتی First Frame یا Gameplay نیامده:

تکرار ادامه پیدا کند.

---

# قانون ذخیره دانش

هر چیزی که پیدا شد باید نوشته شود:

مثال:

```
Problem:
sceKernelWaitSema deadlock


Evidence:
Thread:
AssetGarbageCollectorHelper


Previous:
Import 948


After fix:
Import 131328


Root Cause:
Missing semaphore wake callback


Solution:
Implement guest scheduler callback


Regression:
Dreaming Sarah passed
```

---

# قابلیت Export Knowledge

یک دستور اضافه شود:

```
SharpEmu --export-knowledge PPSA14677
```

خروجی:

```
HarvestDays-KnowledgeTransfer.md
```

که شامل:

* بازی
* TitleID
* موتور
* آخرین وضعیت
* تمام Experiment ها
* تمام Fix ها
* تمام NID های حل شده
* NID های باقی مانده
* Crash signature
* پیشنهاد مرحله بعد

---

# قانون ۲۵ گانه Debug Loop (نسخه جدید)

## 1

هیچ تستی بدون Backup اجرا نشود.

قبل از تغییر:

```
git tag backup-before-expXXX
```

---

## 2

هر Experiment یک فایل جدا داشته باشد.

---

## 3

هیچ خطایی حذف نشود.

Error پاک نمی‌شود.
فقط Status تغییر می‌کند:

```
NEW
ANALYZED
FIXED
REGRESSION_TESTED
```

---

## 4

هر Crash باید Root Cause داشته باشد.

نه:

```
game crashed
```

بلکه:

```
SIGSEGV
Address:
Function:
Cause:
Fix:
```

---

## 5

هر NID ناشناخته ثبت شود.

```
Unknown NID:
Count:
Games affected:
Priority:
```

---

## 6

هر Pointer باید بررسی شود.

خصوصاً:

```
Host Pointer
Guest Pointer
GPU Address
VRAM Address
```

---

## 7

هر Memory Fault باید قبل از Crash تحلیل شود.

---

## 8

هر Thread باید ثبت شود.

مثلا:

```
Unity Worker Thread
GC Thread
Audio Thread
Render Thread
```

---

## 9

Deadlock Detector اضافه شود.

اگر:

```
Thread waiting > 10 sec
```

ثبت کند:

```
Possible Deadlock
```

---

## 10

هر Semaphore باید Trace شود.

ثبت:

```
Create
Wait
Signal
Destroy
```

---

## 11

هر Callback مهم ثبت شود.

خصوصاً:

```
pthread
C11
IL2CPP
Unity
Scheduler
```

---

## 12

قبل از Stub ساختن بررسی شود.

بفهمد:

Fake Stub
یا
Real Implementation

---

## 13

هر Fix باید حداقل روی دو بازی تست شود.

---

## 14

Regression Test اجباری باشد.

بعد از هر تغییر:

Dreaming Sarah
Arise

باید تست شوند.

---

## 15

First Frame معیار اصلی باشد.

نه فقط:

```
No crash
```

---

## 16

Progress Score ذخیره شود.

مثلا:

```
ELF 100%
CRT 90%
IL2CPP 70%
GPU 30%
VideoOut 0%
```

---

## 17

هر بازی Knowledge مستقل داشته باشد.

---

## 18

مشکل مشترک بین بازی‌ها Merge شود.

مثلا:

Harvest Days
Seeker
Yatzi

همه:

```
IL2CPP + semaphore
```

یک Issue شود.

---

## 19

هر تست باید Command قابل تکرار داشته باشد.

مثال:

```
./test-game.sh PPSA14677
```

---

## 20

Log باید قابل خلاصه‌سازی باشد.

از:

100MB log

به:

10 خط علت اصلی

---

## 21

AI Summary تولید شود.

هر Experiment:

```
AI_ANALYSIS.md
```

---

## 22

هیچ دانش مهم فقط داخل Chat نماند.

باید Export شود.

---

## 23

قبل از تغییر بزرگ:

```
CURRENT_STATE.md
```

ذخیره شود.

---

## 24

هر Fix باید توضیح داشته باشد:

```
Why?
How?
Impact?
Risk?
```

---

## 25

هدف نهایی:

```
Crash
 ↓
Boot
 ↓
First Frame
 ↓
Menu
 ↓
Gameplay
```

---

# مواردی که احتمالاً هنوز ندارید و باید اضافه شود

## Memory Intelligence

* Pointer origin tracker
* Host pointer leak detector
* Guest address validator

## CPU Debug

* Register snapshot قبل Crash
* Call stack reconstruction
* Instruction history

## Kernel Debug

* Thread timeline
* Semaphore graph
* Mutex ownership

## IL2CPP Debug

* Static constructor tracker
* Execute_once real callback
* Metadata loader

## GPU Debug

* AGC command replay
* DCB capture
* Shader failure report

## Automation

یک دستور:

```
./bringup-loop.sh PPSA14677
```

خروجی:

```
Experiment 001
Experiment 002
Experiment 003

Until:
First Frame Detected
```

---

به نظر من الان بزرگ‌ترین ارزش برای پروژه شما **نه یک Fix دیگر، بلکه همین Knowledge Transfer + Bringup Loop است**. چون الان شما ۵ بازی دارید که سه تای آنها تقریباً روی یک کلاس مشکل گیر کرده‌اند؛ اگر دانش Harvest Days حل شود احتمال زیاد Seeker و Yatzi هم حرکت می‌کنند.
این گزارش Yatzi یک نکته خیلی مهم دارد: این دیگر یک مشکل اختصاصی یک بازی نیست؛ تبدیل شده به **الگوی مشترک Unity/IL2CPP startup deadlock**.

با داده‌ای که فرستادی، برداشت عملی:

## وضعیت Yatzi

```
ELF Loading        ✅
Imports            ✅
IL2CPP PRX         ✅
Unity Threads      ✅
Asset Workers      ✅
VideoOut           ❌
```

یعنی مشکل دیگر در مراحل اولیه نیست.

نقطه شکست:

```
sceKernelWaitSema
```

این همان خانواده مشکلی است که برای:

* Harvest Days
* Seeker My Shadow
* Yatzi

تکرار شده.

---

## نتیجه مهم برای توسعه اصلی

به جای اینکه برای هر بازی Fix جدا بسازید، باید یک بخش مرکزی ساخته شود:

```
SharpEmu.Diagnostics
        |
        |
        +-- RuntimeDeadlockAnalyzer
              |
              +-- Semaphore Tracker
              +-- Thread Wait Graph
              +-- IL2CPP Init Tracker
              +-- Static Constructor Tracker
              +-- Worker Thread Analyzer
```

---

## چیزی که الان کم دارید

### 1. Semaphore Deadlock Detector ⭐⭐⭐⭐⭐

الان لاگ فقط می‌گوید:

```
sceKernelWaitSema
```

ولی باید بگوید:

```
Thread:
AssetGarbageCollectorHelper #7

Waiting Semaphore:
0x123456

Owner:
Thread #2

Wait duration:
45 seconds

Possible deadlock:
YES
```

---

### 2. Thread Dependency Graph

برای Unity خیلی مهم است:

مثلاً:

```
Main Thread
 |
 +-- IL2CPP Init
       |
       +-- Static Constructor
             |
             +-- Worker Thread
                    |
                    +-- WaitSema
```

این سریع نشان می‌دهد کجا گیر کرده.

---

### 3. Guest Callback Trace

با توجه به این:

```
_Execute_once
```

باید بفهمیم:

```
Unity انتظار دارد:
guest callback اجرا شود

ولی:
HLE فقط stub داده
```

پس باید ثبت شود:

```
Callback requested:
0x801xxxxx

Executed:
NO

Reason:
Scheduler missing
```

---

### 4. First Frame Progress Score

الان خوب دارید:

```
Boot Progress 75%
```

ولی باید استاندارد شود:

مثلاً:

```
Boot Score:

ELF             100%
Imports         100%
CRT             95%
IL2CPP          90%
Threads         100%
Semaphore       40%
GPU             0%
VideoOut        0%
First Frame     0%
```

---

## Knowledge Transfer برای Yatzi

این فایل باید تولید شود:

```
KnowledgeTransfer/Games/Yatzi.md
```

با این محتوا:

```
Game:
Yatzi

TitleID:
PPSA17697

Engine:
Unity IL2CPP

Reached:
Import #131328

Working:
- ELF
- CRT
- IL2CPP
- Unity workers

Blocked:
sceKernelWaitSema

Common With:
- Harvest Days
- Seeker My Shadow

Suspected Fix:
Real semaphore scheduling
Guest callback execution
Execute_once implementation

Do not retry:
Import resolution
CRT stubs
AGC initialization
```

---

## مهم‌ترین نتیجه

Yatzi ثابت کرد که مسیر شما درست است.

چون یک بازی خارجی با نسخه دیگر:

```
Import #131328
```

رسیده.

یعنی مشکل:

❌ "صدها NID کم داریم"

نیست.

مشکل:

✅ Runtime synchronization layer

است.

---

اولویت فعلی من:

1. **sceKernelWaitSema واقعی + Scheduler** ⭐⭐⭐⭐⭐
2. `_Execute_once` واقعی با Guest callback ⭐⭐⭐⭐⭐
3. Thread wait graph ⭐⭐⭐⭐
4. GPU/AGC بعد از عبور startup ⭐⭐⭐

اگر این سه مورد اول درست شوند، احتمالاً Harvest Days + Seeker + Yatzi همزمان جلو می‌روند، چون هر سه یک الگو دارند.
با توجه به کل مسیر SharpEmuT24 که ساختید، من فکر می‌کنم مشکل Harvest Days / Seeker / Yatzi دیگر **مرحله بوت اولیه نیست**. طبق لاگ‌هایی که گفتید الگو این است:

```
ELF Load ✅
Imports Resolve ✅
CRT/C++ ABI ✅
IL2CPP Load ✅
Unity Threads ✅
AssetGarbageCollectorHelper ✅
Static Init ⚠️
Semaphore / Thread Sync ❌
VideoOut ❌
First Frame ❌
```

یعنی احتمال زیاد مشکل در **Runtime Synchronization + Guest Thread Execution + Unity initialization barrier** است.

به تستر اصلی این متن را بده. (یک فایل دستورالعمل واحد باشد، دیتابیس لازم نیست)

---

# SHARPEMU BRINGUP LOOP ENGINE v1.0

## هدف: حل خودکار مشکل بازی تا First Frame

قوانین:

## قانون 1 — هر تغییر قبل از تست باید checkpoint شود

قبل از هر آزمایش:

```
git tag backup/before-exp-XXX
```

هیچ تستی بدون نقطه برگشت انجام نشود.

---

# قانون 2 — هیچ خطایی دور ریخته نشود

هر اجرا باید ذخیره شود:

```
KnowledgeTransfer/
 ├── GameName/
 │    ├── RUN_HISTORY.md
 │    ├── CRASH_REPORT.md
 │    ├── FIX_APPLIED.md
 │    ├── CURRENT_STATE.md
 │    └── latest.log
```

---

# قانون 3 — هر Crash باید تبدیل به Task شود

فرمت:

```
Problem:
Function:
NID:
Register:
Memory:
Phase:
Possible Cause:
Applied Fix:
Result:
```

---

# قانون 4 — فقط یک مشکل در هر آزمایش

اشتباه:

```
10 تغییر
build
test
```

درست:

```
یک تغییر
build
test
compare
```

---

# قانون 5 — قبل از Fix کردن Crash، ریشه پیدا شود

هر SIGSEGV:

بررسی:

```
RIP
RSP
RDI
RSI
RDX
Memory region
Guest/Host pointer
```

---

# قانون 6 — تست Host Pointer Leak

برای تمام توابع pointer-return:

لاگ اضافه شود:

```
RETURN POINTER CHECK

address:
type:
guest:
host:
mapped:
```

اگر:

```
0x55xxxxxxxx
```

بود:

مشکوک HOST POINTER

اگر:

```
0x8xxxxxxxx
```

بود:

Guest Memory

---

# قانون 7 — IL2CPP Initialization Tracking

ثبت شود:

```
IL2CPP START

Loading metadata
Creating threads
Static constructors
ExecuteOnce
TLS
Exception runtime
```

---

# قانون 8 — _Execute_once واقعی بررسی شود

مشکل احتمالی:

الان:

```
stub return success
```

ولی Unity انتظار دارد:

```
run callback once
block others
release waiting threads
```

تست:

پیاده سازی واقعی:

```
GuestThreadExecution.Scheduler.TryCallGuestFunction()
```

---

# قانون 9 — sceKernelWaitSema بررسی شود

چون چند بازی همینجا گیر می‌کنند.

بررسی:

```
sceKernelCreateSema
sceKernelWaitSema
sceKernelSignalSema
sceKernelPollSema
```

لاگ:

```
semaphore id
current count
requested count
thread id
owner
timeout
```

---

# قانون 10 — Deadlock Detector اضافه شود

اگر:

یک thread بیشتر از:

```
30 seconds
```

منتظر بود:

گزارش:

```
POSSIBLE DEADLOCK

Thread:
Waiting:
Semaphore:
Created by:
Last signal:
```

---

# قانون 11 — Thread Scheduler Trace

ثبت:

```
Thread Create
Thread Start
Thread Exit
Thread Wait
Thread Resume
```

---

# قانون 12 — TLS کامل بررسی شود

Unity شدیداً TLS استفاده می‌کند.

تست:

```
pthread_getspecific
pthread_setspecific
__tls_get_addr
```

ثبت:

```
TLS key
Thread
Address
Value
```

---

# قانون 13 — C++ Exception Runtime

بررسی:

```
__cxa_allocate_exception
__cxa_throw
__cxa_begin_catch
__cxa_end_catch
```

---

# قانون 14 — Memory Guard

قبل از memcpy/memset:

بررسی:

```
source mapped?
destination mapped?
size valid?
permission?
```

---

# قانون 15 — Heap Tracking

هر malloc:

ثبت:

```
address
size
caller
thread
```

هر free:

ثبت:

```
free address
allocated from
```

---

# قانون 16 — Missing NID Intelligence

هر NID:

ذخیره:

```
NID
name
calls count
first call
last call
return value
```

---

# قانون 17 — Return Value Validation

برای HLE:

بررسی:

```
NULL
negative
invalid pointer
wrong handle
```

---

# قانون 18 — AGC Pipeline

برای بازی‌های جدید:

بررسی:

```
sceAgcCreateShader
sceAgcCreatePipeline
sceAgcSubmitDcb
Dcb parser
Register state
Framebuffer write
```

---

# قانون 19 — VideoOut فقط بعد از GPU آماده شود

ثبت:

```
Swapchain created
Image acquired
Command submitted
Present called
```

---

# قانون 20 — First Frame Detector

هر اجرا:

جستجو:

```
non black pixels
framebuffer change
present event
```

---

# قانون 21 — Regression Test

بعد از هر Fix:

اجرا:

```
Dreaming Sarah
Arise
Harvest Days
Seeker
Yatzi
```

نباید بازی قبلی خراب شود.

---

# قانون 22 — AI Summary خروجی بدهد

آخر هر Loop:

فایل:

```
AI_CONTEXT_NEXT_RUN.md
```

بسازد.

شامل:

```
Current state
Solved problems
Remaining blockers
Next experiments
```

---

# قانون 23 — حلقه خودکار

فرآیند:

```
RUN GAME

↓

COLLECT LOG

↓

ANALYZE

↓

FIND ROOT CAUSE

↓

CREATE FIX PLAN

↓

APPLY ONE FIX

↓

BUILD

↓

TEST AGAIN

↓

UPDATE KNOWLEDGE
```

تا رسیدن به:

```
FIRST FRAME PRESENTED
```

---

# قانون 24 — هر موفقیت تبدیل به Knowledge شود

مثلا:

```
HarvestDays.md

Problem:
sceKernelWaitSema

Fix:
Real semaphore scheduler

Result:
VideoOut reached
```

---

# قانون 25 — هیچ وقت از صفر شروع نشود

همیشه:

```
LAST_KNOWN_GOOD_STATE
```

مبنا باشد.

---

# تست‌های اولویت‌دار من برای حل مشکل فعلی:

ترتیب پیشنهادی:

## EXP-009

### Semaphore Investigation

احتمال: ⭐⭐⭐⭐⭐

---

## EXP-010

### Real _Execute_once callback

احتمال: ⭐⭐⭐⭐⭐

---

## EXP-011

### Guest Thread Scheduler

احتمال: ⭐⭐⭐⭐

---

## EXP-012

### TLS + pthread کامل

احتمال: ⭐⭐⭐⭐

---

## EXP-013

### IL2CPP Static Constructor Trace

احتمال: ⭐⭐⭐⭐

---

## EXP-014

### Heap/Pointer Leak Detector

احتمال: ⭐⭐⭐

---

## EXP-015

### AGC Framebuffer Validation

احتمال: ⭐⭐⭐

---

خروجی نهایی هر تست فقط یک فایل بده:

```
KnowledgeTransfer/GameName/latest-transfer.md
```

که چت اصلی بتواند مستقیم بخواند و روی فورک اعمال کند.

---

به نظر من الان بزرگ‌ترین شانس حل سه بازی:

1. `sceKernelWaitSema`
2. `_Execute_once`
3. `GuestThreadExecution.Scheduler`

هستند، چون سه بازی مستقل همان نقطه گیر را نشان داده‌اند.
با توجه به کاری که تا الان کردید، مشکل اصلی این است که **تسترها فقط اجرا می‌کنند ولی یک چرخه علمی تشخیص → ثبت → تغییر → تست مجدد → انتقال دانش ندارند**. باید یک سیستم حلقه‌ای بسازید که بدون دیتابیس هم کار کند و همه چیز را داخل فایل نگه دارد.

برای نسخه اصلی (نه تستر) این قابلیت‌ها را اضافه کنید:

---

# دستور ساخت سیستم Bringup Loop + Knowledge Export

این متن را مستقیم به برنامه‌نویس اصلی بده:

```
هدف:
ساخت یک سیستم استاندارد Debug Bringup Loop برای SharpEmu که بدون دیتابیس کار کند و تمام دانش حل مشکل را داخل فایل ذخیره کند.

قوانین:

1. هر اجرای بازی باید یک Experiment ID داشته باشد.

مثال:
EXP-001
EXP-002
EXP-003


ساختار:

SharpEmu.Diagnostics/
├── Bringup/
│   ├── Experiments/
│   │   ├── EXP-001/
│   │   │   ├── input.json
│   │   │   ├── run.log
│   │   │   ├── crash-analysis.md
│   │   │   ├── fixes.md
│   │   │   └── result.md
│   │
│   └── CURRENT_STATE.md
│
├── KnowledgeTransfer/
│   ├── Games/
│   ├── FixHistory.md
│   ├── KnownIssues.md
│   └── Export.md
```

---

# قانون اصلی حلقه تست

```
RUN
 ↓
COLLECT LOG
 ↓
ANALYZE
 ↓
IDENTIFY ROOT CAUSE
 ↓
WRITE SOLUTION
 ↓
CREATE PATCH PLAN
 ↓
APPLY FIX
 ↓
BUILD
 ↓
RUN AGAIN
 ↓
COMPARE RESULT
```

تا وقتی First Frame یا Gameplay نیامده:

تکرار ادامه پیدا کند.

---

# قانون ذخیره دانش

هر چیزی که پیدا شد باید نوشته شود:

مثال:

```
Problem:
sceKernelWaitSema deadlock


Evidence:
Thread:
AssetGarbageCollectorHelper


Previous:
Import 948


After fix:
Import 131328


Root Cause:
Missing semaphore wake callback


Solution:
Implement guest scheduler callback


Regression:
Dreaming Sarah passed
```

---

# قابلیت Export Knowledge

یک دستور اضافه شود:

```
SharpEmu --export-knowledge PPSA14677
```

خروجی:

```
HarvestDays-KnowledgeTransfer.md
```

که شامل:

* بازی
* TitleID
* موتور
* آخرین وضعیت
* تمام Experiment ها
* تمام Fix ها
* تمام NID های حل شده
* NID های باقی مانده
* Crash signature
* پیشنهاد مرحله بعد

---

# قانون ۲۵ گانه Debug Loop (نسخه جدید)

## 1

هیچ تستی بدون Backup اجرا نشود.

قبل از تغییر:

```
git tag backup-before-expXXX
```

---

## 2

هر Experiment یک فایل جدا داشته باشد.

---

## 3

هیچ خطایی حذف نشود.

Error پاک نمی‌شود.
فقط Status تغییر می‌کند:

```
NEW
ANALYZED
FIXED
REGRESSION_TESTED
```

---

## 4

هر Crash باید Root Cause داشته باشد.

نه:

```
game crashed
```

بلکه:

```
SIGSEGV
Address:
Function:
Cause:
Fix:
```

---

## 5

هر NID ناشناخته ثبت شود.

```
Unknown NID:
Count:
Games affected:
Priority:
```

---

## 6

هر Pointer باید بررسی شود.

خصوصاً:

```
Host Pointer
Guest Pointer
GPU Address
VRAM Address
```

---

## 7

هر Memory Fault باید قبل از Crash تحلیل شود.

---

## 8

هر Thread باید ثبت شود.

مثلا:

```
Unity Worker Thread
GC Thread
Audio Thread
Render Thread
```

---

## 9

Deadlock Detector اضافه شود.

اگر:

```
Thread waiting > 10 sec
```

ثبت کند:

```
Possible Deadlock
```

---

## 10

هر Semaphore باید Trace شود.

ثبت:

```
Create
Wait
Signal
Destroy
```

---

## 11

هر Callback مهم ثبت شود.

خصوصاً:

```
pthread
C11
IL2CPP
Unity
Scheduler
```

---

## 12

قبل از Stub ساختن بررسی شود.

بفهمد:

Fake Stub
یا
Real Implementation

---

## 13

هر Fix باید حداقل روی دو بازی تست شود.

---

## 14

Regression Test اجباری باشد.

بعد از هر تغییر:

Dreaming Sarah
Arise

باید تست شوند.

---

## 15

First Frame معیار اصلی باشد.

نه فقط:

```
No crash
```

---

## 16

Progress Score ذخیره شود.

مثلا:

```
ELF 100%
CRT 90%
IL2CPP 70%
GPU 30%
VideoOut 0%
```

---

## 17

هر بازی Knowledge مستقل داشته باشد.

---

## 18

مشکل مشترک بین بازی‌ها Merge شود.

مثلا:

Harvest Days
Seeker
Yatzi

همه:

```
IL2CPP + semaphore
```

یک Issue شود.

---

## 19

هر تست باید Command قابل تکرار داشته باشد.

مثال:

```
./test-game.sh PPSA14677
```

---

## 20

Log باید قابل خلاصه‌سازی باشد.

از:

100MB log

به:

10 خط علت اصلی

---

## 21

AI Summary تولید شود.

هر Experiment:

```
AI_ANALYSIS.md
```

---

## 22

هیچ دانش مهم فقط داخل Chat نماند.

باید Export شود.

---

## 23

قبل از تغییر بزرگ:

```
CURRENT_STATE.md
```

ذخیره شود.

---

## 24

هر Fix باید توضیح داشته باشد:

```
Why?
How?
Impact?
Risk?
```

---

## 25

هدف نهایی:

```
Crash
 ↓
Boot
 ↓
First Frame
 ↓
Menu
 ↓
Gameplay
```

---

# مواردی که احتمالاً هنوز ندارید و باید اضافه شود

## Memory Intelligence

* Pointer origin tracker
* Host pointer leak detector
* Guest address validator

## CPU Debug

* Register snapshot قبل Crash
* Call stack reconstruction
* Instruction history

## Kernel Debug

* Thread timeline
* Semaphore graph
* Mutex ownership

## IL2CPP Debug

* Static constructor tracker
* Execute_once real callback
* Metadata loader

## GPU Debug

* AGC command replay
* DCB capture
* Shader failure report

## Automation

یک دستور:

```
./bringup-loop.sh PPSA14677
```

خروجی:

```
Experiment 001
Experiment 002
Experiment 003

Until:
First Frame Detected
```

---

به نظر من الان بزرگ‌ترین ارزش برای پروژه شما **نه یک Fix دیگر، بلکه همین Knowledge Transfer + Bringup Loop است**. چون الان شما ۵ بازی دارید که سه تای آنها تقریباً روی یک کلاس مشکل گیر کرده‌اند؛ اگر دانش Harvest Days حل شود احتمال زیاد Seeker و Yatzi هم حرکت می‌کنند.
این گزارش Yatzi یک نکته خیلی مهم دارد: این دیگر یک مشکل اختصاصی یک بازی نیست؛ تبدیل شده به **الگوی مشترک Unity/IL2CPP startup deadlock**.

با داده‌ای که فرستادی، برداشت عملی:

## وضعیت Yatzi

```
ELF Loading        ✅
Imports            ✅
IL2CPP PRX         ✅
Unity Threads      ✅
Asset Workers      ✅
VideoOut           ❌
```

یعنی مشکل دیگر در مراحل اولیه نیست.

نقطه شکست:

```
sceKernelWaitSema
```

این همان خانواده مشکلی است که برای:

* Harvest Days
* Seeker My Shadow
* Yatzi

تکرار شده.

---

## نتیجه مهم برای توسعه اصلی

به جای اینکه برای هر بازی Fix جدا بسازید، باید یک بخش مرکزی ساخته شود:

```
SharpEmu.Diagnostics
        |
        |
        +-- RuntimeDeadlockAnalyzer
              |
              +-- Semaphore Tracker
              +-- Thread Wait Graph
              +-- IL2CPP Init Tracker
              +-- Static Constructor Tracker
              +-- Worker Thread Analyzer
```

---

## چیزی که الان کم دارید

### 1. Semaphore Deadlock Detector ⭐⭐⭐⭐⭐

الان لاگ فقط می‌گوید:

```
sceKernelWaitSema
```

ولی باید بگوید:

```
Thread:
AssetGarbageCollectorHelper #7

Waiting Semaphore:
0x123456

Owner:
Thread #2

Wait duration:
45 seconds

Possible deadlock:
YES
```

---

### 2. Thread Dependency Graph

برای Unity خیلی مهم است:

مثلاً:

```
Main Thread
 |
 +-- IL2CPP Init
       |
       +-- Static Constructor
             |
             +-- Worker Thread
                    |
                    +-- WaitSema
```

این سریع نشان می‌دهد کجا گیر کرده.

---

### 3. Guest Callback Trace

با توجه به این:

```
_Execute_once
```

باید بفهمیم:

```
Unity انتظار دارد:
guest callback اجرا شود

ولی:
HLE فقط stub داده
```

پس باید ثبت شود:

```
Callback requested:
0x801xxxxx

Executed:
NO

Reason:
Scheduler missing
```

---

### 4. First Frame Progress Score

الان خوب دارید:

```
Boot Progress 75%
```

ولی باید استاندارد شود:

مثلاً:

```
Boot Score:

ELF             100%
Imports         100%
CRT             95%
IL2CPP          90%
Threads         100%
Semaphore       40%
GPU             0%
VideoOut        0%
First Frame     0%
```

---

## Knowledge Transfer برای Yatzi

این فایل باید تولید شود:

```
KnowledgeTransfer/Games/Yatzi.md
```

با این محتوا:

```
Game:
Yatzi

TitleID:
PPSA17697

Engine:
Unity IL2CPP

Reached:
Import #131328

Working:
- ELF
- CRT
- IL2CPP
- Unity workers

Blocked:
sceKernelWaitSema

Common With:
- Harvest Days
- Seeker My Shadow

Suspected Fix:
Real semaphore scheduling
Guest callback execution
Execute_once implementation

Do not retry:
Import resolution
CRT stubs
AGC initialization
```

---

## مهم‌ترین نتیجه

Yatzi ثابت کرد که مسیر شما درست است.

چون یک بازی خارجی با نسخه دیگر:

```
Import #131328
```

رسیده.

یعنی مشکل:

❌ "صدها NID کم داریم"

نیست.

مشکل:

✅ Runtime synchronization layer

است.

---

اولویت فعلی من:

1. **sceKernelWaitSema واقعی + Scheduler** ⭐⭐⭐⭐⭐
2. `_Execute_once` واقعی با Guest callback ⭐⭐⭐⭐⭐
3. Thread wait graph ⭐⭐⭐⭐
4. GPU/AGC بعد از عبور startup ⭐⭐⭐

اگر این سه مورد اول درست شوند، احتمالاً Harvest Days + Seeker + Yatzi همزمان جلو می‌روند، چون هر سه یک الگو دارند.



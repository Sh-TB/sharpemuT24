# EXP-145 — 38000 Mutex Classification + Producer Reachability + Dispatch Loop Trace

**Date:** 2026-08-04
**Status:** Producer function NEVER REACHED — dispatch loop IS reached

---

## Step 1: Mutex Loop Classification

### Result: Case B — Different mutexes (IL2CPP type initialization)

The 38000+ `scePthreadMutexLock` calls are **38000+ DIFFERENT calls** with incrementing import numbers (Import#2000, #3000, ..., #40000). This is NOT a spin loop on the same mutex.

**This is IL2CPP type/class initialization** — each type has its own mutex for thread-safe static initialization. Processing thousands of types from `global-metadata.dat` requires thousands of mutex operations.

**VERDICT: Normal IL2CPP behavior. Not the blocker.**

---

## Step 2: Producer Address Correction

### EXP-137 had the WRONG offset:
- EXP-137 reported: `eboot.bin @ 0x159D52` (address `0x800159D52`)
- EXP-145 found: `eboot.bin @ 0x15DD52` (address `0x80015DD52`)
- The instruction `inc dword [r14+0x90]` = `41 FF 86 90 00 00 00` was found at offset `0x15DD52`, NOT `0x159D52`

### Producer function at 0x80015DCD0:
```
push rbp
mov rbp, rsp
push r15; push r14; push r12; push rbx
sub rsp, 0x10
...
call 0x80009BA50        ; get allocator
call [rbx+0x10]         ; allocate memory
call 0x80009DA90        ; get item from queue
cmp ecx, eax            ; capacity vs count
jbe grow_queue
mov [rcx+rax*8], rbx    ; store item
inc dword [r14+0x90]    ; ← PRODUCER (increment job counter)
jmp done
```

### Single caller at 0x8001EDB28:
```
lea r13d, [r9+r9+0xF]   ; calculate count
and r13d, 0xFFFFFFF0    ; align to 16
cmp r13d, 0x4001        ; check if too large
jb skip                 ; if too large, skip
mov rdi, [r12]          ; queue pointer
mov esi, r13d           ; count
call 0x80015DCD0        ; ← CALL PRODUCER
```

---

## Step 3: Producer Reachability Test

### INT3 instrumentation:
- Slot 1: Producer function entry (`0x80015DCD0`) — **0 hits** ❌
- Slot 2: Producer caller (`0x8001EDB28`) — **0 hits** ❌
- Slot 3: Dispatch loop entry (`0x804F6E9E6`) — **2 hits** ✅

### Dispatch loop hits:
```
Hit 1: rip=0x804F6E9E7 rdi=0x1 rsi=0x1 rax=0x0
Hit 2: rip=0x804F6E9E7 rdi=0x1 rsi=0x1 rax=0xFFFFFFFF
```

### Conclusion:
- **The producer function is NEVER called** — the bootstrap job submission code is never reached
- **The producer caller is NEVER called** — the code that should call the producer is skipped
- **The dispatch loop IS reached** — twice, from the main thread

---

## Step 4: Dispatch Loop Analysis

The dispatch loop at `0x804F6E9E6` is called twice:
1. First call: `rdi=0x1, rsi=0x1, rax=0x0` — initial entry
2. Second call: `rdi=0x1, rsi=0x1, rax=0xFFFFFFFF` — after processing

After the second hit, the INT3 patch corrupted the CALL instruction (original byte `0xE8`), causing an Illegal Instruction fault. This is an instrumentation artifact, not a guest bug.

---

## State Machine

```
STATE 0: Package loaded                    ✅ PASS
STATE 1: PRX modules loaded                ✅ PASS
STATE 2: global-metadata.dat loaded        ✅ PASS
STATE 3: IL2CPP initialized                ✅ PASS
STATE 4: Unity runtime initialization      ✅ PASS
STATE 5: Job System bootstrap              ❌ BLOCKED
STATE 6: Render bootstrap                  Not reached

Current State: 4
Expected Next State: 5
Missing Transition: Bootstrap job submission (producer at 0x80015DCD0 never called)
```

---

## First Divergence Point

```
Expected:
  Create semaphores → Call producer (0x80015DCD0) → Enter dispatch loop → WaitSema(0x81)

Actual:
  Create semaphores → Enter dispatch loop → WaitSema(0x81)
                     ↑
                     Producer call SKIPPED
```

The producer caller at `0x8001EDB28` is **never reached**. Something in the execution path before `0x8001EDB28` causes the code to skip the producer call.

---

## Next Steps

1. **Disassemble the code path leading to 0x8001EDB28** — find what function should call it
2. **Find the conditional branch that skips the producer caller** — what condition evaluates wrong?
3. **Check if the 38000+ mutex loop should eventually reach 0x8001EDB28** — maybe the loop exits early
4. **Trace the main thread's RIP between mkdir and dispatch loop entry** — find the exact divergence point

---

## Golden Gate Status

Dreaming Sarah: ✅ PASS (23/23/245 colors) — no regression

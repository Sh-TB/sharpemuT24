# EXP-167 RESULT

## Execution order:

1. 0x8007E8790 (initializer) — executed FIRST (evidence: [0x801E50DF0] and [0x801E50DF8] are non-zero at clear write time)
2. 0x800804175 (clear write) — executed SECOND (INT3 hit, [0x801E50DF0]=0x801BB4B77, [0x801E50DF8]=0x801E518C8)
3. 0x8013FCE40 (parent function) — executed THIRD (calls consumer at +0xDF9)
4. 0x8013EB6B0 (consumer) — executed FOURTH (INT3 hit, [0x801E518C8]=0x0)

## Initializer: Executed

Evidence: At the clear write breakpoint (0x800804175), [0x801E50DF0]=0x801BB4B77 and [0x801E50DF8]=0x801E518C8. These values are set by function 0x8007E8790 at offsets +0x29 and +0x53. Since they are non-zero at the clear write time, 0x8007E8790 must have executed BEFORE the clear write, which itself executes before the consumer.

## Consumer: Executed

Evidence: INT3 at 0x8013EB6B0 was hit. At that point, [0x801E518C8]=0x0 (NULL).

## Memory (at consumer entry):

0x801E50DF0 = 0x0000000801BB4B77
0x801E50DF8 = 0x0000000801E518C8
0x801E518C8 = 0x0000000000000000

## Hypothesis: REJECTED

Reason: The initializer 0x8007E8790 DOES execute before the consumer 0x8013EB6B0. This is proven by the fact that [0x801E50DF0] and [0x801E50DF8] are already initialized to non-zero values when the clear write (0x800804175) executes, and the clear write executes before the consumer. The initializer correctly sets up the pointer chain ([0x801E50DF8] = address of 0x801E518C8), but the VALUE at [0x801E518C8] is never set because function 0x8013FCE40 takes an early branch (jl at +0x91, r13d=1 < 2) that skips the init write at +0x24E. The problem is NOT ordering — the initializer runs first. The problem is that the init write inside the parent function is skipped due to the r13d < 2 condition.

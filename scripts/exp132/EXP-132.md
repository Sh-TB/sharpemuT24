# EXP-132 — Multi-Branch Validation Before Fix

**Date:** 2026-08-04
**Status:** COMPLETE — EXP-131 HYPOTHESIS REJECTED

## Finding

**EXP-131's hypothesis (RELA table loading failure) is REJECTED.** Runtime logs confirm:
- `TryLoadTableBytes: loaded from guest memory at 0x801F435F0` — RELA table loaded SUCCESSFULLY
- `Processing 50450 relocations...` — ALL 49,850 RELA + 600 JMPREL entries processed
- `Found 605 unique NIDs, 50450 descriptors` — ALL descriptors created
- Zero relocation errors/warnings in log

**EXP-130's claim that the producer pointer is NULL is UNVERIFIED.** EXP-130 checked the FILE value (offset `0x1d00cb0` = `0x0`), but this is the pre-relocation value. SharpEmu applies relocations to GUEST MEMORY, not to the file. The file being `0x0` is EXPECTED and NORMAL.

**The runtime value at `0x801cfccb0` might be `0x801028d80` (correctly relocated).** We have NO evidence it's NULL at runtime.

## Tests Executed

### TEST A — Confirm RELA loading
- **RELA table loaded: YES** (`TryLoadTableBytes: loaded from guest memory at 0x801F435F0`)
- **Relocations processed: 50,450** (49,850 RELA + 600 JMPREL)
- **Descriptors created: 50,450**
- **Relocation errors: 0**
- **CONCLUSION: RELA loading SUCCEEDED. EXP-131 hypothesis REJECTED.**

### TEST B — Working game comparison
- Cannot run Dreaming Sarah/Arise (no game files available in this session)
- **UNKNOWN** — need separate test

### TEST C — Manual relocation injection
- Cannot execute (no dotnet SDK to rebuild)
- **UNKNOWN**

### TEST D — Missing RELA targets audit
- EXP-130 checked file values (all 0x0 pre-relocation) — this is EXPECTED
- No runtime memory dump available to verify actual patched values
- **UNKNOWN** — need runtime memory dump or INT3 trace

### TEST E — Relocation ordering
- RELA table IS in guest memory (loaded from `[---]` segment which SharpEmu maps despite flags=0)
- Target address `0x1cfccb0` IS in `[RW-]` segment (mapped and writable)
- `CanAccessAddress` checks readability (TryRead), not writability — should succeed for `[RW-]`
- **Ordering is CORRECT** — segments mapped before relocations applied

### TEST F — Fix attempt
- No fix needed — the bug is NOT in `TryLoadTableBytes`
- **NOT APPLICABLE**

## Evidence

### Runtime log confirms RELA loading succeeded

```
[LOADER] Dynamic Info: Rela=0x1F435F0, RelaSize=0x124170
[LOADER] TryLoadTableBytes: trying location=0x1F435F0, size=0x124170, imageBase=0x800000000
[LOADER] TryLoadTableBytes: trying guest address 0x801F435F0
[LOADER] TryLoadTableBytes: loaded from guest memory at 0x801F435F0
[LOADER] Processing 50450 relocations...
[LOADER] Found 605 unique NIDs, 50450 descriptors
[LOADER] Created 605 import stubs
```

### EXP-130's file-value check was misleading

EXP-130 checked the file at offset `0x1d00cb0` and found `0x0`. This is the PRE-RELOCATION value in the ELF file — it's supposed to be `0x0`. SharpEmu writes the relocated value to GUEST MEMORY at `0x801cfccb0`, not to the file.

## Hypothesis Status

| Hypothesis | Status | Evidence |
|-----------|--------|----------|
| RELA table loading failure | **REJECTED** | Log confirms successful loading |
| R_X86_64_RELATIVE not supported | REJECTED (EXP-131) | Code handles it correctly |
| Producer pointer NULL at runtime | **UNKNOWN** | File value is 0x0 (expected); runtime value not verified |
| Producer never called despite correct pointer | **UNKNOWN** | Need runtime trace of 0x801cfccb0 reads |

## What we actually know

1. RELA table loaded successfully (50,450 entries)
2. RELATIVE relocation code is correct
3. Target address is in a writable segment
4. File value is 0x0 (PRE-RELOCATION — expected)
5. Runtime value at 0x801cfccb0 is UNKNOWN
6. Producer function at 0x801028d80 is never called (no RIP in 0x801028xxx range in logs)
7. Main thread blocks on WaitSema(0x81) in the PRX dispatch loop

## Remaining hypotheses

1. **The producer pointer IS correctly patched, but the code that reads and calls it is never reached** (70% confidence) — The dispatch loop in the PRX blocks before the eboot code that reads from 0x801cfccb0 can execute.

2. **The producer pointer is NOT patched despite the relocation being processed** (30% confidence) — Some edge case in the relocation application (e.g., TryWriteRelocationValue fails silently) could leave it NULL.

## Next Experiment

**EXP-133 (proposed):**

Two approaches:

**Approach A (runtime memory dump):** Add a temporary diagnostic that dumps the value at guest address `0x801cfccb0` AFTER all relocations are applied. This would definitively confirm whether the producer pointer is `0x801028d80` (correct) or `0x0` (NULL).

**Approach B (static analysis):** Trace what code reads from address `0x801cfccb0` (or the GOT/data table containing it). The producer function pointer is stored at this address. Some code should load it and call it. If that code is never reached, the issue is in the CALLER, not the relocation.

**STOP — awaiting user review before EXP-133.**

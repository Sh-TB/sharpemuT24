# EXP-144 — VideoOut / Equeue / GPU / AGC Validation

**Date:** 2026-08-04
**Task ID:** 144
**Predecessor:** EXP-126 (vblank hypothesis rejected), EXP-137 Phase 3A (VideoOut never reached)
**Priority:** 🟢 P3 — only after bootstrap is resolved (EXP-138, 139, 140)
**Status:** STATIC ANALYSIS COMPLETE — runtime tests PENDING

## Goal

Per user:
> "فقط بعد از حل بالا."
> (Only after solving the above.)

This EXP validates VideoOut/equeue/GPU/AGC behavior AFTER the Unity Job System bootstrap is unblocked. Currently Yatzi never reaches VideoOut (all pipeline counters = 0), so this EXP is gated on EXP-138/139/140 success.

---

## Test Plan

### 144.1 VideoOut Event — ✅ STATIC-COMPLETE (from EXP-137 Phase 4)
- `sceVideoOutAddFlipEvent` (NID `Xru92wHJRmg`?) — implemented in `VideoOutExports.cs:578`
- Implementation registers flip events on the equeue
- Yatzi imports this NID in eboot.bin (confirmed via strings search)

**Current status at Yatzi stall:** VideoOutAddFlipEvent count = 0 in pipeline counters (Yatzi never reaches VideoOut init).

### 144.2 Equeue — ✅ STATIC-COMPLETE (from EXP-137 Phase 4)
- `sceKernelWaitEqueue` (NID `fzyMKs9kim0`) — implemented in `KernelEventQueueCompatExports.cs:379`
- **KNOWN BUG (Phase 3 of EXP-137 HLE audit):** When `timeoutAddress == 0` AND `RequestCurrentThreadBlock` returns false (non-cooperative caller), returns OK with 0 events. This causes the caller to spin instead of blocking.
- For Yatzi, this bug is downstream — Yatzi never reaches `sceKernelWaitEqueue` due to the upstream deadlock.

### 144.3 SubmitFlip — ✅ STATIC-COMPLETE
- `sceVideoOutSubmitFlip` — implemented in `VideoOutExports.cs`
- Implementation triggers `TriggerFlipEvents()` which calls `KernelEventQueueCompatExports.TriggerDisplayEvent` for each registered flip event

**Current status at Yatzi stall:** VideoOutSubmitFlip count = 0.

### 144.4 DCB Growth — ⏸ PENDING (requires bootstrap unblocked)
**Test:** After bootstrap is unblocked:
- Before first frame: `AgcDcbDrawIndexAuto = 0`
- After first frame: `AgcDcbDrawIndexAuto` should increase

If DCB count remains 0 after bootstrap unblocked, the GPU/AGC layer has its own bug.

### 144.5 AGC Memory — ✅ STATIC-COMPLETE
- `sceAgcDriverMapMemory` — implemented
- `sceAgcDriverSubmitDcb` — implemented
- Both at `AgcExports.cs`

**Current status at Yatzi stall:** Both counts = 0 (Yatzi never reaches AGC init).

### 144.6 DCB execution — ⏸ PENDING (requires bootstrap unblocked)
**Test:** After bootstrap is unblocked:
- DCB parse OK?
- DCB execute?
- Framebuffer write?

If DCB parses but doesn't execute, there's a GPU command buffer bug.
If DCB executes but no framebuffer write, there's a render target binding bug.

---

## My Additional Considerations

### 144.7 Arise GPU memory fault (per AI_CONTEXT.md)
Per AI_CONTEXT.md, Arise previously showed a "GPU memory fault" — this is a different game, different bug, but informs what to watch for in Yatzi post-bootstrap.

**Test:** After Yatzi bootstrap unblocked, watch for:
- `sceAgcDriverMapMemory` failures
- GPU memory faults (UNMAPPED)
- DCB parse errors

### 144.8 Dreaming Sarah baseline (per CHECKPOINT_v0.0.11.md)
Per CHECKPOINT, Dreaming Sarah pipeline counters (20s run):
- `AgcInit` = 1
- `AgcCreateShader` = 99
- `AgcCreatePrimState` = 378
- `AgcDriverSubmitDcb` = 84
- `AgcDcbDrawIndexAuto` = 66
- `AgcDcbDrawIndexOffset` = 120
- `VideoOutSubmitFlip` (direct) = 0
- `VideoOutAddFlipEvent` = 84
- frames = 90

**Yatzi target (after fix):** similar pipeline counter profile (Unity may differ — e.g., Unity uses DCB-embedded flips, Dreaming Sarah uses DCB-embedded flips per CHECKPOINT §18).

### 144.9 Frame #2 milestone
Per user's EXP-144 goal:
> "اثبات: آیا بعد از رفع bootstrap: Frame #2 شروع می‌شود یا نه."
> (Proof: After bootstrap is unblocked, does Frame #2 start?)

**Success criterion:** After bootstrap fix, Yatzi produces at least 2 frames (Frame #1 + Frame #2). Pipeline counters should show:
- `AgcInit` ≥ 1
- `VideoOutOpen` ≥ 1
- `VideoOutSubmitFlip` ≥ 2 OR `AgcDriverSubmitDcb` ≥ 2

### 144.10 Verify sceKernelWaitEqueue fix is needed
Per EXP-137 HLE audit, `sceKernelWaitEqueue` has a known bug for non-cooperative callers. After Yatzi bootstrap unblocked, Yatzi may reach this code path.

**Test:** If Yatzi stalls at `sceKernelWaitEqueue` (after bootstrap fix), apply the fix recommended in EXP-137 HLE audit:
- Replace fall-through `return OK` with indefinite `Monitor.Wait` (host path)
- OR return `ORBIS_GEN2_ERROR_TRY_AGAIN` so caller retries

### 144.11 Check `sceVideoOutAddVblankEvent`
Per EXP-126, this NID was searched and NOT FOUND in either eboot or PRX. CONFIRMED — Yatzi does NOT use vblank events. Only uses flip events.

---

## Status Summary

| Subtest | Status | Notes |
|---------|--------|-------|
| 144.1 VideoOut Event | ✅ STATIC-COMPLETE | sceVideoOutAddFlipEvent implemented |
| 144.2 Equeue | ✅ STATIC-COMPLETE | sceKernelWaitEqueue has known bug for non-cooperative callers |
| 144.3 SubmitFlip | ✅ STATIC-COMPLETE | Implemented, triggers flip events |
| 144.4 DCB Growth | ⏸ PENDING | Needs bootstrap unblocked |
| 144.5 AGC Memory | ✅ STATIC-COMPLETE | Implemented |
| 144.6 DCB execution | ⏸ PENDING | Needs bootstrap unblocked |
| 144.7 Arise GPU memory fault | ⏸ PENDING (watch for) | Different game, same warning |
| 144.8 Dreaming Sarah baseline | ✅ STATIC-COMPLETE | Reference pipeline counters |
| 144.9 Frame #2 milestone | ⏸ PENDING | Ultimate success criterion |
| 144.10 sceKernelWaitEqueue fix | ⏸ PENDING (conditional) | Apply if Yatzi stalls here post-bootstrap |
| 144.11 sceVideoOutAddVblankEvent | ✅ COMPLETE | Not used by Yatzi (per EXP-126) |

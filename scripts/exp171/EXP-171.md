# EXP-171 RESULT

## 1. Confirmed Execution Order (with argc=2)

```
Loader → eboot entry (0x800000070) → DT_INIT (0x800000010)
  → 0x8013FCE40 (parent, argc=2, jl NOT taken)
  → 0x8013EB6B0 (consumer, [0x801E518C8]=0x00000020000259C0)
  → Consumer passes branches: +0x72, +0x277, +0x2B6, +0x38E, +0x3CD, +0xB75, +0xB82, +0x12B2, +0x1326, +0x1786, +0x18E0, +0x191F
  → Consumer exits between +0x191F and +0x2A1E (init write at +0x3969 NEVER reached)
  → Dispatch loop → WaitSema(0x81) → DEADLOCK
```

## 2. Confirmed Missing Initialization

**[0x801E51240] = 0x0 (NULL)** — NEVER initialized in any runtime case.

### Producers:
- **0x8007FD8F9** (clear, func 0x8007FD320): `mov qword [0x801E51240], 0` — **EXECUTES** (writes 0)
- **0x8013EF019** (init, inside consumer at +0x3969): `mov [0x801E51240], rax` — **NEVER EXECUTES** (consumer exits before +0x3969)
- **0 PRX writes**

### Classification:
- Clear writer: A) Executed
- Init writer: C) Skipped by branch (consumer exits between +0x191F and +0x2A1E)

## 3. Exact Branch Causing Failure

**The consumer exits between +0x191F and +0x2A1E.**

Runtime evidence:
- +0x191F (jne): HIT — rax=0, jne NOT taken
- +0x2A1E (jle): NOT HIT — execution never reaches this point

Between +0x191F and +0x2A1E, there are:
- Multiple CALL instructions (0x801936700, 0x8007E2280, 0x801936980, etc.)
- An unconditional JMP at +0x1B37: `jmp +0x1BF5`
- Multiple conditional branches (+0x1A05 jne, +0x1A27 je, etc.)

The exact exit point is likely one of:
1. A CALL that doesn't return (e.g., calls dispatch loop directly)
2. A conditional branch at +0x1A05 (jne +0x1BF5) or +0x1B65 (je +0x2D8C)
3. The unconditional JMP at +0x1B37

**Most likely**: The function calls a subroutine that enters the dispatch loop, OR a conditional branch redirects to a path that eventually reaches the dispatch loop without writing [0x801E51240].

## 4. Closed Hypotheses

| Hypothesis | Status | Evidence |
|-----------|--------|----------|
| argc=1 is the only blocker | CLOSED | argc=2 fixes [0x801E518C8] but [0x801E51240] still NULL |
| [0x801E518C8] NULL causes all downstream failures | CLOSED | With argc=2, [0x801E518C8] is non-NULL but deadlock persists |
| Consumer early exit at +0x72 is the blocker | CLOSED | With argc=2, +0x72 is NOT taken, consumer continues |
| First 4 skip branches (+0x277 to +0x3CD) are taken | CLOSED | All 4 are NOT taken (rax=0, jne not taken) |
| Branches +0xB75 to +0x191F are taken | CLOSED | All are NOT taken, execution passes through |
| [0x801E51240] has no init writer | CLOSED | Init writer exists at +0x3969 but is never reached |

## 5. Next Evidence Target

**Find the exact exit point between +0x191F and +0x2A1E.**

Set INT3 at:
- +0x1A05 (jne +0x1BF5) — first conditional after +0x191F
- +0x1B37 (jmp +0x1BF5) — unconditional jump
- +0x1B65 (je +0x2D8C) — conditional that skips far ahead

Determine which path leads to the dispatch loop without reaching +0x3969.
